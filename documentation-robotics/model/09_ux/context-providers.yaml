app-state-provider:
  id: ux.context-provider.app-state-provider
  name: App State Provider
  type: context-provider
  documentation: Root provider that composes all state contexts (Theme, Socket,
    System, Project, Agent)
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: web_ui/src/contexts/AppStateProvider.jsx
            symbol: AppStateProvider
theme-provider:
  id: ux.context-provider.theme-provider
  name: Theme Provider
  type: context-provider
  documentation: Manages dark/light theme state with localStorage persistence
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: web_ui/src/contexts/ThemeContext.jsx
            symbol: ThemeProvider
socket-provider:
  id: ux.context-provider.socket-provider
  name: Socket Provider
  type: context-provider
  documentation: Manages WebSocket connection, real-time events, Claude logs,
    medic events, and system stats
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: web_ui/src/contexts/SocketContext.jsx
            symbol: SocketProvider
system-state-provider:
  id: ux.context-provider.system-state-provider
  name: System State Provider
  type: context-provider
  documentation: Manages system health and circuit breaker state with polling intervals
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: web_ui/src/contexts/SystemStateContext.jsx
            symbol: SystemStateProvider
project-state-provider:
  id: ux.context-provider.project-state-provider
  name: Project State Provider
  type: context-provider
  documentation: Manages project data and configurations with polling intervals
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: web_ui/src/contexts/ProjectStateContext.jsx
            symbol: ProjectStateProvider
agent-state-provider:
  id: ux.context-provider.agent-state-provider
  name: Agent State Provider
  type: context-provider
  documentation: Manages agent state from pipeline runs and provides agent control
    operations (kill)
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: web_ui/src/contexts/AgentStateContext.jsx
            symbol: AgentStateProvider
