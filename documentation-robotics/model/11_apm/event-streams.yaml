redis-pubsub-stream:
  id: apm.event-stream.redis-pubsub-stream
  name: Redis Pub/Sub Event Stream
  type: event-stream
  documentation: Redis-backed real-time event streaming with pub/sub for live
    observability and Redis Streams for historical replay with configurable
    maxlen for memory management
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: monitoring/observability.py
            symbol: ObservabilityManager.emit
