decision-analytics:
  id: apm.analytics-engine.decision-analytics
  name: Decision Analytics Engine
  type: analytics-engine
  documentation: Analytics engine for observability decision events with pattern
    detection (bottlenecks, failures, long review cycles), metrics aggregation
    (review cycles, routing, errors), and health scoring across time windows
  properties:
    source:
      reference:
        provenance: extracted
        locations:
          - file: monitoring/decision_analytics.py
            symbol: DecisionAnalytics
