# Core dependencies
asyncio-throttle>=1.0.2
aiofiles>=23.2.1
redis>=5.0.1
pydantic>=2.4.2
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Monitoring
python-json-logger>=2.0.7
psutil>=5.9.6
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-http>=1.25.0

# Pattern Detection / Analytics
elasticsearch>=8.11.0
docker>=7.0.0  # For Medic Docker log monitoring

# Web/API
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
eventlet>=0.35.0  # Async backend for Flask-SocketIO
requests>=2.31.0
httpx>=0.25.0  # For MCP server communication
fastapi>=0.111.0
uvicorn>=0.30.0
mcp[cli]>=1.2.0,<2.0.0

# YAML processing
pyyaml>=6.0.1

# Date/time handling
python-dateutil>=2.8.2

# Scheduling
apscheduler>=3.10.4

# Testing
pytest>=8.0.0
pytest-asyncio>=1.0.0
# Supplies the per-test timeout configured in pytest.ini (#204). Without it
# pytest does not even recognise the `timeout` ini key, so the suite has no
# bound on a wedged test. Verified against pytest 9.1.1 with 2.4.0.
#
# NEW IN #204, AND `--strict-config` MAKES ITS ABSENCE FATAL: an image built
# before #204 merged does not have it, and running the suite there collects
# everything and then exits 4 with nothing run. Both images install from this
# file (Dockerfile:102, Dockerfile.agent:47), so they must be rebuilt --
# `docker compose build orchestrator` first, since Dockerfile.agent is
# `FROM switchyard-orchestrator:latest`. CI is unaffected: unit-tests.yml
# pip-installs this file on every run.
pytest-timeout>=2.4.0

# GitHub Integration
PyGithub>=1.59.1
PyJWT>=2.8.0

# Anthropic Claude
anthropic>=0.7.7

# Development Tools
black>=23.11.0
isort>=5.12.0
mypy>=1.7.1

# Security
cryptography>=41.0.7

# CLI Tools
click>=8.1.7
rich>=13.7.0