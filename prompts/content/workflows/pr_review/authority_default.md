---
invoked_by: pipeline/pr_review_stage.py — _build_verification_prompt() via default_loader.workflow_template("pr_review/authority_default")
  Used when authority_key is not "business_analyst" or "software_architect" (e.g. parent_issue or unknown).
  Injected as {authority_framing} in verification_main.md.
variables: none
---

## Context Authority: Acceptance Criteria

This context source is the **source of truth for acceptance criteria**.
Every explicit requirement listed here must be implemented.
Flag any missing or partially implemented requirements.
